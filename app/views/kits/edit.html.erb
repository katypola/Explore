<% if user_signed_in? && current_user.admin? %>

<div id="editKit">
	<h1>Editing Kit</h1>
	<%= render 'form', kit: @kit %>
	<%= link_to 'See Kit', @kit %> |
	<%= link_to 'See All Kits', kits_path %>
</div>

<% end %>